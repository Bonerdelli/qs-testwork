/**
 * Root node identifier
 */
const TREE_ROOT_NODE_ID = 1

/**
 * Maximum depth of initially loaded tree (including root level)
 */
const TREE_MAX_INITIAL_DEPTH = 4

module.exports = {
  TREE_ROOT_NODE_ID,
  TREE_MAX_INITIAL_DEPTH,
}
